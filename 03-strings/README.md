# Strings in Python

This folder contains Python programs and exercises focused on **strings and string manipulation**.

Strings are one of the most commonly used data types in Python. These exercises provide practical experience with creating, accessing, modifying, and processing strings.

## 📚 Topics Covered

- Introduction to Strings
- Creating Strings
- Single and Double Quotes
- Multiline Strings
- String Indexing
- Negative Indexing
- String Slicing
- String Concatenation
- String Repetition
- String Length
- String Methods
- String Formatting
- f-Strings
- Escape Characters
- Membership Operators
- Comparing Strings
- String Traversal
- Basic String Problem Solving

## 🔤 Important String Methods

Some commonly used Python string methods practiced in this folder include:

- `upper()`
- `lower()`
- `capitalize()`
- `title()`
- `strip()`
- `replace()`
- `split()`
- `join()`
- `find()`
- `count()`
- `startswith()`
- `endswith()`

## 💻 Practice Programs

The exercises in this folder include programs such as:

- Concatenate two strings
- Find the length of a string
- Reverse a string
- Check whether a string is a palindrome
- Count vowels in a string
- Count characters in a string
- Convert a string to uppercase and lowercase
- Remove spaces from a string
- Replace characters in a string
- Find the occurrence of a character
- Check whether a substring exists
- Count words in a sentence
- Work with string slicing
- Practice different string methods

## 🎯 Learning Objectives

By completing these exercises, I aim to:

- Understand how strings work in Python
- Learn string indexing and slicing
- Use built-in string methods
- Perform different string operations
- Solve common string-based programming problems
- Improve problem-solving and coding skills

## 🛠️ Technologies Used

- **Language:** Python 3
- **Editor:** VS Code / Any Python IDE
- **Version Control:** Git & GitHub

## 📌 Purpose

These exercises are part of my Python learning journey and are based on exercises from the **Udemy Python course**.

This folder focuses on developing a strong understanding of Python strings through hands-on practice.

---

⭐ **Practice → Learn → Improve → Repeat**
